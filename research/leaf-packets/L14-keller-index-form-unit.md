# Keller Index-Form Unit

- **Priority:** `P0`
- **Status:** `DISPOSED — LITERATURE_BOUND_RANK_THREE_EXCLUSION`
- **Dependencies:** `CLM-001`, `CLM-002`, `CLM-029`, `CLM-030`, `CLM-031`, `CLM-034`, `CLM-058`, `CLM-059`, `CLM-062`–`CLM-066`, `CLM-074`–`CLM-078`
- **Authority:** `MUTABLE_NONAUTHORITATIVE`
- **Issue:** [#3](https://github.com/snissn/planar-jacobian/issues/3)

## Current rank-three packet

The packet [`../issues/rank-three-boundary-cube-unit/`](../issues/rank-three-boundary-cube-unit/README.md) records `CLM-074` at `literature_bound` scope, conditional refinements `CLM-075`–`CLM-077` at `candidate_proved` scope, and the source-open terminal `CLM-078` at `literature_bound` scope. Its pinned local adversarial review is not independent scientific acceptance. Merge location does not confer scientific authority.

## Load-bearing question

At function-field degree three, either construct one integral trace-free section `s` with fixed-section index `Fitt_0(O/B[s])=B`, equivalently `Phi(s) in C*`, or prove that the simultaneous rank-three Keller hypotheses are inconsistent. Proving only that the universal coefficient/content ideal is `B` is insufficient.

## Accepted evidence

`CLM-074` supplies the bounded terminal: field degree three gives generic sheet number three, and Orevkov's audited 1987 primary theorem excludes a three-sheeted polynomial map with nonzero constant Jacobian. The simultaneous hypotheses are therefore inconsistent. This is a literature-bound exclusion, not a constructed unit-index section.

## Forbidden shortcuts

Local monogenicity, generic separation, dimension counts, rationality, smoothness, fixed-sheet monodromy, squarefree branch, or the existence of an open affine plane do not substitute for the unit equation. Adding a base element to a primitive element changes no sheet difference. The rank-three exclusion must not be promoted to degree four or higher, global monogenicity, degree one, or `JC_2`.

## Required artifacts

The completed packet states the field-degree/generic-sheet bridge, binds the external theorem to its primary source, preserves the conditional binary-cubic calculations and countermodel mutations, identifies the exact scientific nonclaims, and records the pinned local adversarial review. `R3BC-07` remains packet-local and is not synchronized as a global claim.

## Stop rule

Satisfied at rank-three scope by the literature-bound incompatibility result `CLM-074`. The leaf is disposed without claiming a unit section. Reopening requires a concrete error in the field-degree bridge or Orevkov source binding, not a desire for a constructive proof.

## Handoff

Preserve `CLM-075`–`CLM-078` at their recorded statuses as conditional tools and corrections. No successor leaf is created from this disposition: any degree-four-or-higher investigation must be separately scoped, sourced, and queued, and no edge from `TERM-RANK-THREE-EXCLUSION` may reach the degree-one or `JC_2` terminal chain.
