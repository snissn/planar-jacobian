# Track I — Exact Symplectic Boundary Constraints

> Status: `MUTABLE_NONAUTHORITATIVE`

In two dimensions `dP∧dQ=dx∧dy`, and `P dQ + y dx` is a polynomial exact one-form. The graph is an exact algebraic Lagrangian.

### Potential leverage

- zero logarithmic residues along boundary divisors;
- constraints on leading terms of source coordinates under boundary valuations;
- compatibility with the canonical divisor/different;
- conductor moment conditions at cusp-like points.

### Warning

Residue cancellation does not eliminate higher-order poles. Exactness alone does not make the canonical divisor vanish.

### Exit

Derive a theorem that the complete principal-part package is incompatible with a nonempty normalization boundary.

## Integrated one-boundary successor (2026-07-24)

The one-boundary successor retains the full Laurent equations and adds the leading relation `n a' b-m a b'=0`. It explicitly rejects the inference from one boundary and zero residue to weighted homogeneity or pole elimination. The remaining fixed-type conductor/principal-part system is `CLM-072`.

## Integrated Liouville successor (2026-07-27)

The issue #5 Laurent-conductor packet records `CLM-086`–`CLM-094`. It
normalizes a source pole to `x=s^(-m)`, solves the complete triangular
coefficient recursion, kills the unique logarithmic resonance, and traces the
order-zero primitive down to prove `P dQ=dR` on the normalized target branch.
Thus the Liouville-nonexact ramified pole-supported subclass is excluded.

The explicit Liouville-exact non-toric near-model survives all displayed
formal equations but fails polynomial realization. The exact-symplectic exit
is therefore not reached. `CLM-094` remains open for a higher
differential/conductor obstruction or a proof that the survivor cannot be
realized by global polynomial Keller data.
