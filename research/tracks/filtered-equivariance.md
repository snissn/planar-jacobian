# Compatibility Pointer: Filtered Equivariance

The canonical rich track is [`m-filtered-equivariance.md`](m-filtered-equivariance.md).

This path is retained for sparse-mainline links and historical branch compatibility. Scientific status is controlled by `research/claim_ledger.json` and the canonical track.

The exact reviewed scope and pinned revision are recorded in the canonical track and freeze record; this compatibility path does not broaden them.
