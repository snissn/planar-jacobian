# Contributing

Use one issue-scoped branch per proof leaf. Keep claims scoped, update machine-readable ledgers with prose, run `python3 scripts/validate_repository.py`, and submit exact source bindings for literature-dependent statements. Do not merge an unconditional theorem claim without independent mathematical review.
